# UniSpace-AI

> Next.js + Spring Boot 全栈示例项目

该仓库演示了一个典型的**前后端分离**架构：前端使用 [Next.js 16](https://nextjs.org/)，后端使用 [Spring Boot 4](https://spring.io/projects/spring-boot)。两端通过 HTTP/JSON 进行通信，彼此可以完全独立地开发、部署与扩缩容。

---

## 目录结构

```
.
├── backend/   # Spring Boot 后端服务
│   ├── src/   # Java 源码 & 资源
│   ├── build.gradle.kts
│   └── ...
└── frontend/  # Next.js + React 前端
    ├── src/
    ├── public/
    ├── package.json
    └── ...
```

> 说明：两端互不依赖，可以分开启动，也可以分别部署到不同的服务器 / PaaS。

---

## 技术栈

| 层        | 技术                 | 说明 |
|-----------|----------------------|------|
| 前端      | Next.js 16、React 19 | React 服务器组件 (App Router) + TypeScript + TailwindCSS |
| 后端      | Spring Boot 4        | 仅使用 Spring Web (无数据库依赖) |
| 构建工具  | pnpm / npm / yarn，Gradle | 统一的脚本让启动更简单 |
| 运行环境  | Node.js ≥ 18，JDK 21 | 建议使用 `asdf` / `nvm` / `sdkman` 进行版本管理 |

---

## 快速开始

> 以下命令假设你位于项目根目录。

### 1. 克隆仓库

```bash
git clone <repo-url>
cd UniSpace-AI
```

### 2. 一键启动脚本

仓库已提供 `start.sh`，自动并行启动前后端（需先确保本地已安装 Node.js ≥18 与 JDK 21）：

```bash
./start.sh  # Linux/macOS
```

脚本会：
1. 在 `backend/` 运行 `./gradlew bootRun`（后端监听 8080）
2. 在 `frontend/` 安装依赖后执行 `npm run dev`（前端监听 3000）

按 `Ctrl+C` 可同时停止两端服务。

---

如果想分别手动启动，也可继续参考下列命令：

```bash
# 后端
cd backend
./gradlew bootRun   # Windows 请使用 gradlew.bat

# 前端（另一个终端）
cd frontend
npm install
a npm run dev
```

默认会监听在 `http://localhost:8080`。

### 3. 启动前端 (Next.js)

新开一个终端窗口：

```bash
cd frontend
npm install       # 或 pnpm install / yarn
npm run dev       # 启动开发服务器，默认端口 3000
```

此时访问 <http://localhost:3000> 即可看到欢迎页面，点击按钮会调用后端接口。

> 如果端口冲突，可通过环境变量 `PORT` 覆盖默认端口，例如 `PORT=4000 npm run dev`。

---

## 生产构建

```
# 前端
cd frontend
npm run build      # 生成 .next 输出
npm run start      # 使用 Next.js 内置服务器

# 后端
cd backend
./gradlew bootJar  # 生成可执行 JAR（build/libs/*.jar）
java -jar build/libs/workflow-0.0.1-SNAPSHOT.jar
```

你可以将前端构建后的静态资源托管在 CDN / Nginx，也可以让 Next.js 继续以 Node 方式运行。

---

## API 设计

当前仅提供演示接口：

| 方法 | 路径            | 说明           |
|------|-----------------|----------------|
| GET  | `/api/hello`    | 返回 `{ "message": "Hello, world!" }` |

> 如果想要扩展业务，可在 `backend/src/main/java/...` 里根据领域模型添加更多 `@RestController`。

---

## 环境变量 & 配置

| 位置                 | 变量/配置            | 作用                           |
|----------------------|----------------------|--------------------------------|
| backend `application.properties` | `server.port`         | 修改后端端口 (默认 8080) |
| frontend `.env.local`            | `NEXT_PUBLIC_API_BASE`| 可替换接口基础路径 (可选) |

---

## 常见问题

1. **`CORS` 错误**
   * Spring Boot 默认已开启 `@CrossOrigin` (参见后端代码)。若访问出现跨域提示，请确认端口与域名是否一致，或在后端新增全局 `CorsConfiguration`。

2. **端口被占用**
   * 修改 `application.properties` 中的 `server.port`（后端）或设置环境变量 `PORT`（前端）。

3. **使用其他包管理器**
   * 所有 npm 脚本兼容 `npm` / `pnpm` / `yarn`，按个人习惯选择即可。

---

## 贡献指南

1. Fork 本仓库后创建特性分支：`git checkout -b feature/awesome`
2. 提交你的更改：`git commit -m "feat: add awesome feature"`
3. 推送分支：`git push origin feature/awesome`
4. 创建 Pull Request 📮

---

## License

[MIT](LICENSE) © 2026 UniSpace Team

