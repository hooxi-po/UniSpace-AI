# UniSpace-AI

> 基于 Cesium 的校园地下管网三维可视化系统

前后端分离架构：前端使用 [Nuxt.js](https://nuxt.com/) + [Cesium](https://cesium.com/)，后端使用 [Spring Boot](https://spring.io/projects/spring-boot)。

---

## 功能特性

- 🗺️ **三维地图可视化** - 基于 Cesium 的 3D 地球渲染
- 🏢 **建筑模型展示** - OSM 建筑数据 3D 渲染，支持点击查看建筑信息
- 🔧 **管网管理系统** - 供水、污水、排水管道的可视化与管理
- 📍 **道路数据转管道** - 自动从 GeoJSON 道路数据生成管道网络
- ✏️ **管道绘制工具** - 在地图上交互式绘制管道路径
- 📊 **实时数据监控** - 压力监测点数据展示
- 🎛️ **图层控制** - 灵活的图层显示/隐藏控制

---

## 目录结构

```
.
├── backend/                    # Spring Boot 后端服务
│   ├── src/
│   ├── build.gradle.kts
│   └── ...
└── frontend/                   # Nuxt.js (Vue) 前端
    ├── app/
    ├── components/
    │   ├── CesiumViewer.vue    # Cesium 地图主组件
    │   └── ui/                 # UI 组件
    ├── composables/
    │   └── useMapState.ts      # 全局状态管理
    ├── utils/cesium/           # Cesium 工具模块
    │   ├── buildings.ts        # 建筑数据处理
    │   ├── pipes.ts            # 管道渲染
    │   ├── roads.ts            # 道路数据转管道
    │   ├── viewer.ts           # Viewer 初始化
    │   └── ...
    ├── public/
    │   ├── cesium/             # Cesium 静态资源
    │   └── map/
    │       └── map.geojson     # 福建理工大学地图数据
    └── ...
```

---

## 技术栈

| 层        | 技术                    | 说明 |
|-----------|-------------------------|------|
| 前端      | Nuxt.js 4, Vue 3        | TypeScript + 自动导入 |
| 地图引擎  | Cesium                  | 3D 地球可视化 |
| 后端      | Spring Boot 4           | Spring Web |
| 构建工具  | npm/pnpm, Gradle        | |
| 运行环境  | Node.js ≥ 18, JDK 21    | |

---

## 快速开始

### 环境准备

1. 配置 Cesium Ion Token（在 `frontend/.env` 中设置）：
```
NUXT_PUBLIC_CESIUM_TOKEN=your_cesium_ion_token
```

### 一键启动

```bash
./start.sh
```

脚本会并行启动：
- 后端: http://localhost:8080
- 前端: http://localhost:3000

按 `Ctrl+C` 停止所有服务。

### 手动启动

```bash
# 后端
cd backend
./gradlew bootRun

# 前端（另一个终端）
cd frontend
npm install
npm run dev
```

---

## 核心功能说明

### 道路数据转管道

系统会自动从 `public/map/map.geojson` 中提取道路数据并转换为管道：

| 道路类型 | 管道类型 | 颜色 |
|----------|----------|------|
| secondary, primary | 供水管道 | 绿色 |
| unclassified, residential, service | 排水管道 | 蓝色 |
| footway, path, pedestrian | 污水管道 | 棕色 |

### 管道绘制

1. 点击左侧边栏「管网编辑器」
2. 点击「添加管道」
3. 点击「在地图上绘制」按钮
4. 在地图上点击添加节点，双击完成绘制

---

## 生产构建

```bash
# 前端
cd frontend
npm run build
npm run preview

# 后端
cd backend
./gradlew bootJar
java -jar build/libs/workflow-0.0.1-SNAPSHOT.jar
```

---

## 数据来源

- 地图数据：OpenStreetMap（福建理工大学旗山校区）
- 建筑数据：Cesium OSM Buildings

---

## License

[MIT](LICENSE) © 2026 UniSpace Team
